enum Language {en,ar}

enum Gender { male, female }

enum SortOptions { date, title, source }

enum SortOrder { asc, desc }

enum RequestState {initial,loading,done,error}

enum CategoryId {halls,dresses,suits,hair,travel,makeup_artist,photographer,cafe_and_restaurant}